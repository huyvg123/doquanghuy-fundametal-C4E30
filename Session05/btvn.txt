1. Why should we use functions at all?
Hàm là một khối mã có thể sử dụng lại có tổ chức, được sử dụng để thực hiện một hành động liên quan duy nhất. Các chức năng cung cấp tính mô đun tốt hơn cho ứng dụng của bạn và mức độ tái sử dụng mã cao.
2. How to define/declare a function?
- Bạn có thể xác định các chức năng để cung cấp các chức năng cần thiết. Dưới đây là các quy tắc đơn giản để xác định hàm trong Python.

+ Các khối chức năng bắt đầu bằng từ khóa def theo sau là tên hàm và dấu ngoặc đơn (()).

+ Bất kỳ tham số hoặc đối số đầu vào nào cũng phải được đặt trong các dấu ngoặc đơn này. Bạn cũng có thể định nghĩa các tham số bên trong các dấu ngoặc đơn này.

+ Câu lệnh đầu tiên của hàm có thể là câu lệnh tùy chọn - chuỗi tài liệu của hàm hoặc chuỗi doc .

+ Khối mã trong mỗi hàm bắt đầu bằng dấu hai chấm (:) và được thụt lề.

+ Câu lệnh return [biểu thức] thoát khỏi một hàm, tùy ý gửi lại một biểu thức cho người gọi. Một câu lệnh return không có đối số giống như return return.
3. How to call/use a function?
ta chỉ cần gọi tên Hàm xong rồi in ra ở cuối dòng
4. What is return, why and how do we use it?
return là 1 lệnh trong hàm, có thể có 1 hoặc nhiều lệnh return. khi câu lệnh chạy đến return, lập tức hàm sẽ không được thực hiện nữa
5. Do we have to use return in every function?
Không vì có 3 dạng là: hàm có hoặc không tham số, hàm có tham số không return, hàm có tham số có cả return
6. What are function arguments/parameters, why and how we use it?
nó là các tham số trong hàm và khi in thhif phải áp dụng câu lệnh :     def function_name(thamso): 
7. How to use function from a different file other than our currently working file?
ta dùng lệnh : from file import function